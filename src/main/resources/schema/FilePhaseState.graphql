type  FilePhaseState {
     id:ID
     current:Boolean
     phaseAgent:FilePhaseAgent
     state:PhaseState
     blockings:[Blocking]
}
input  FilePhaseStateInput {
     id:ID
     current:Boolean
     phaseAgents:[FilePhaseAgentInput]
     state:PhaseStateInput
     blockings:[BlockingInput]
}
