[
  {
    "name": "Validator can change the fileTaskState if he is linked to the activity",
    "target": "action == 'CHANGE_FILE_TASK_STATE' && subject.role.name == 'VALIDATOR'",
    "condition": "resource.fileActivity.activity.id == subject.activity.id"
  },
  {
    "name": "Validator can read file activity if he is assigned to it",
    "target": "action == 'READ_FILE_ACTIVITY' && subject.role.name == 'VALIDATOR'",
    "condition": "resource.activity.id == subject.activity.id"
  },
  {
    "name": "Validator can read file task he is assigned to the task activity",
    "target": "'READ_FILE_TASK' == action && subject.role.name == 'VALIDATOR'",
    "condition": "resource.fileActivity.activity.id == subject.activity.id"
  },
  {
    "name": "Validator can change file activity data if he is linked to it",
    "target": "'UPDATE_FILE_ACTIVITY_DATA_FIELD' == action && subject.role.name == 'VALIDATOR'",
    "condition": "resource.activity.id == subject.activity.id"
  }
]
